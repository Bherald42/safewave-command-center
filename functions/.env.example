# Copy to functions/.env and fill in (non-secret settings only).
# The mailbox PASSWORD is a secret — set it with:
#   firebase functions:secrets:set IONOS_PASSWORD
IONOS_EMAIL=sales@safewavetech.com
IMAP_HOST=imap.ionos.com
SMTP_HOST=smtp.ionos.com
